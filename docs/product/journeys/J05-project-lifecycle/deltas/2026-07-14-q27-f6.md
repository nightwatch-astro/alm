> **MIGRATED:** current truth now lives at
> `docs/journeys/J05-project-lifecycle/journey.md`.
> This delta is frozen legacy history: it records behavior as understood on
> its own date and may not match current behavior.
> See `docs/product/journeys/README.md`.

# q27-f6 — J05-project-lifecycle: Project reopens via completed→processing on attribution add

Task: q27-f6 · Status: merged-spec
Wave 0 grilling: Q27 · framing layer (008 US5–US7, FR-013–FR-022; cross-spec 006/009)

## Behavior delta (this journey)

Adding attributed subs to a completed project reopens it through the spec-009 `completed → processing` lifecycle edge (with the reopen warning).

## Stages hit

- Stage 4/5 area — a completed project can transition back to processing when new subs are attributed to it.

## Reviewer verification

1. Pick the add+reopen candidate; assert the project lifecycle moves completed → processing and the new session is a framing member.
2. Negative: the reopen honors the raw-subs-archived revoke/warn (Q25).

## Rerun set (minimal)

- Layer-1: `lifecycle_integrity` (journeys.rs)
- Layer-2: `projects_ui_wizard_creates_real_folders_and_blocks_duplicate_name` (lifecycle_ui_journeys.rs)
- Manual-Windows: `journey-05-*` (project lifecycle)
- Coverage-matrix: #7, #8, #9
