> **MIGRATED:** current truth now lives at
> `docs/journeys/J10-settings-appearance-i18n/journey.md`.
> This delta is frozen legacy history: it records behavior as understood on
> its own date and may not match current behavior.
> See `docs/product/journeys/README.md`.

# q15-t122 — J10-settings-appearance-i18n: Settings mutations write durable audit rows

Task: q15-t122 · Status: merged-spec
Wave 0 grilling: Q15 · durable audit unification (#647, FR-130–FR-134, spec §8.3)

## Behavior delta (this journey)

Every durable-data settings change now writes a durable `audit_log_entry` row (before→after), not only a bus event. Noisy keys (e.g. `pattern`) are audited once at the committed value, never per keystroke.

## Stages hit

- Stage 1 "Settings groups … panes … Every pane auto-saves" — an autosave of a durable-data key now produces a durable audit row.

## Reviewer verification

1. Change a durable-data setting (e.g. Calibration offset tolerance); assert a new `audit_log_entry` row exists with `actor=user`, `severity=workflow`, and a before→after pair in `payload`.
2. Type into the Naming `pattern` builder rapidly, then commit: exactly ONE audit row at the final value (negative: no per-keystroke rows).
3. Toggle a UI-state-only key (`rememberFollowLogs`): assert NO `audit_log_entry` row (FR-134 exemption).

## Rerun set (minimal)

- Layer-1: to be written in the owning task
- Layer-2: `settings_ui_ingestion_toggle_autosaves_no_global_save_button` (settings_journeys.rs) — extend to assert the audit row
- Manual-Windows: `journey-10-*` stage 1
- Coverage-matrix: #19, #22
